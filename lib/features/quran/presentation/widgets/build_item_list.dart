import 'package:flutter/material.dart';

class BuildItemList extends StatelessWidget {
  const BuildItemList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
